namespace Mubarrat.Modem.Enums;

/// <summary>
/// Enumeration indicating the direction of the data (received or sent).
/// </summary>
public enum DataDirection
{
    /// <summary>
    /// Indicates data has been received from the serial port.
    /// </summary>
    Received,

    /// <summary>
    /// Indicates data has been sent through the serial port.
    /// </summary>
    Sent
}
